import { FormValidationPipePipe } from './form-validation-pipe.pipe';

describe('FormValidationPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormValidationPipePipe();
    expect(pipe).toBeTruthy();
  });
});
